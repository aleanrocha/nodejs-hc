<section class="sec-users">
  <h2>Usuários</h2>
  <div class="users">
  {{#each users}}
    <div class="user">
      <span>#{{this.id}}</span>
      <h3>{{this.name}}</h3>
      <a href="/user/{{this.id}}">Ver mais</a>
    </div>
  {{/each}}
  </div>
</section>